zuul:
  routes:
    robbin:
      path: /robbin/**
      service-id: consumer-user
    feign:
      path: /feign/**
      service-id: consumer-feign
eureka:
  instance:
    hostname: api-gateway
security:
   user:
    name: test
    password: test
    role: -USER